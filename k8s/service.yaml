apiVersion: v1
kind: Service
metadata:
  name: sm-devsecops
  namespace: sm
  labels:
    app: sm-devsecops
    platform: sm-suite
spec:
  type: ClusterIP
  selector:
    app: sm-devsecops
  ports:
  - name: http
    port: 8340
    targetPort: http
    protocol: TCP
